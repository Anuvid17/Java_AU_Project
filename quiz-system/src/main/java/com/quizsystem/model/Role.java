package com.quizsystem.model;

/**
 * Enum representing user roles in the quiz system.
 * ADMIN: Can manage categories, quizzes, questions, and view all scores.
 * STUDENT: Can attempt quizzes and view their own scores.
 */
public enum Role {
    ADMIN,
    STUDENT
}
